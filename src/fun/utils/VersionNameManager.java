package fun.utils;

import fun.inject.inject.MinecraftType;
import fun.inject.inject.MinecraftVersion;

import java.util.HashMap;

public class VersionNameManager {
    /*public static HashMap<MinecraftType, HashMap<MinecraftVersion,String>> map=new HashMap<>();
    {
        for(MinecraftType type:MinecraftType.values()){
            map.put(type,new HashMap<>());
        }
    }*/

}
